version: "3.9"
services:
  gpt:
    image: ghcr.io/mlsh0/tg-openai:latest
    container_name: gpt
    networks:
      - proxy
    labels:
      - "traefik.enable=false"
    env_file:
      - ./.env
    restart: unless-stopped
networks:
  proxy:
    external: true
